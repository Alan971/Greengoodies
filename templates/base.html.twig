<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Green Goodies{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🟢</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>Gg</text></svg>"> 
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}"/>
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
            <header>
                <nav>
                    <a href="{{ path('app_home') }}">
                        <img src="{{ asset('img/Logo.png') }}" alt="Green Goodies" class="logo">
                    </a>
                    <ul class="menu">
                        <li><a href="{{ path('app_home') }}">Nos produits</a></li>
                        <li><a href="{{ path('app_basket') }}">Mon panier</a></li>
                        <li><a href="{{ path('app_my_account') }}">Mon compte</a></li>
                        {% if app.user %}
                            <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                        {% else %}
                            <li><a href="{{ path('app_login') }}">Connexion</a></li>
                        {% endif %}
                    </ul>
                </nav>   
            </header>   
        {% block body %}{% endblock %}
        <footer>
            <p> @GreenGoodies - {{ 'now' | date('Y') }} </p>
        </footer>
    </body>
</html>
